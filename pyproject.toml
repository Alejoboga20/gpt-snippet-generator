[tool.poetry]
name = "gpt-snippet-generator"
version = "0.1.0"
description = ""
authors = ["alejoboga-ai <alejo@zelta.ai>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
